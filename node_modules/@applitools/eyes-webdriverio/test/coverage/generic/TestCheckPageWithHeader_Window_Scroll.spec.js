// Generated by @applitools/sdk-coverage-tests
const {remote} = require('webdriverio')
const {
    By,
    Eyes,
    BatchInfo,
    RectangleSize,
    StitchMode,
    VisualGridRunner,
    Target,
    Region,
    FileLogHandler,
  } = require('../../../index')
const path = require('path')

describe('Coverage Tests', () => {
  let eyes
  let driver
  let runner
  beforeEach(async () => {
    const browserOptions = {
        logLevel: 'error',
        capabilities: {
          browserName: 'chrome',
          'goog:chromeOptions': {
            args: ['--headless'],
          },
        },
      }
    browserOptions.hostname = 'localhost'
    browserOptions.port = 4444
    browserOptions.path = '/wd/hub'
    driver = await remote(browserOptions)
    runner = undefined
    eyes = new Eyes()
    undefined
    eyes.setStitchMode(StitchMode.SCROLL)
    eyes.setBranchName('master')
    eyes.setHideScrollbars(true)
    eyes.setApiKey(process.env.APPLITOOLS_API_KEY_SDK)
  })
  afterEach(async () => {
    await driver.deleteSession()
  })
  it('TestCheckPageWithHeader_Window_Scroll', async () => {
    await driver.url('https://applitools.github.io/demo/TestPages/PageWithHeader/index.html')
    await eyes.open(driver, 'Eyes Selenium SDK - Page With Header', 'TestCheckPageWithHeader_Window_Scroll', RectangleSize.parse('700x460'))
    {
    let _checkSettings = Target.window()
        .fully(false)
        .ignoreCaret()
    await eyes.check(undefined, _checkSettings)
    }
    await eyes.close(true)
  })
})