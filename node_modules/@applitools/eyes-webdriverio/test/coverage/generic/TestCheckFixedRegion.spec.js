// Generated by @applitools/sdk-coverage-tests
const {remote} = require('webdriverio')
const {
    By,
    Eyes,
    BatchInfo,
    RectangleSize,
    StitchMode,
    VisualGridRunner,
    Target,
    Region,
    FileLogHandler,
  } = require('../../../index')
const path = require('path')

describe('Coverage Tests', () => {
  let eyes
  let driver
  let runner
  beforeEach(async () => {
    const browserOptions = {
        logLevel: 'error',
        capabilities: {
          browserName: 'chrome',
          'goog:chromeOptions': {
            args: ['--headless'],
          },
        },
      }
    browserOptions.hostname = 'localhost'
    browserOptions.port = 4444
    browserOptions.path = '/wd/hub'
    driver = await remote(browserOptions)
    runner = undefined
    eyes = new Eyes()
    eyes.setStitchMode(StitchMode.CSS)
    undefined
    eyes.setBranchName('master')
    eyes.setHideScrollbars(true)
    eyes.setApiKey(process.env.APPLITOOLS_API_KEY_SDK)
  })
  afterEach(async () => {
    await driver.deleteSession()
  })
  it('TestCheckFixedRegion', async () => {
    await driver.url('http://applitools.github.io/demo/TestPages/fixed-position')
    await eyes.open(driver, 'Eyes Selenium SDK - Fluent API', 'TestCheckFixedRegion', RectangleSize.parse('700x460'))
    {
    let _checkSettings
    _checkSettings
          ? _checkSettings.region(By.css('#fixed'))
          : (_checkSettings = Target.region(By.css('#fixed')))
    _checkSettings.fully(false)
    await eyes.check(undefined, _checkSettings)
    }
    await eyes.close(true)
  })
})