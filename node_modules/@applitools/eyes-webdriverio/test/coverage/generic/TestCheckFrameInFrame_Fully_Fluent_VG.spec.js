// Generated by @applitools/sdk-coverage-tests
const {remote} = require('webdriverio')
const {
    By,
    Eyes,
    BatchInfo,
    RectangleSize,
    StitchMode,
    VisualGridRunner,
    Target,
    Region,
    FileLogHandler,
  } = require('../../../index')
const path = require('path')

describe('Coverage Tests', () => {
  let eyes
  let driver
  let runner
  beforeEach(async () => {
    const browserOptions = {
        logLevel: 'error',
        capabilities: {
          browserName: 'chrome',
          'goog:chromeOptions': {
            args: ['--headless'],
          },
        },
      }
    browserOptions.hostname = 'localhost'
    browserOptions.port = 4444
    browserOptions.path = '/wd/hub'
    driver = await remote(browserOptions)
    runner = runner = new VisualGridRunner(10)
    eyes = new Eyes(runner)
    undefined
    undefined
    eyes.setBranchName('v1')
    eyes.setApiKey(process.env.APPLITOOLS_API_KEY_SDK)
  })
  afterEach(async () => {
    await driver.deleteSession()
  })
  it('TestCheckFrameInFrame_Fully_Fluent_VG', async () => {
    await driver.url('https://applitools.github.io/demo/TestPages/FramesTestPage/')
    await eyes.open(driver, 'Eyes Selenium SDK - Fluent API', 'TestCheckFrameInFrame_Fully_Fluent_VG', RectangleSize.parse('700x460'))
    {
    let _checkSettings
    (_checkSettings = Target.frame(By.css('[name="frame1"]')))
    _checkSettings.frame(By.css('[name="frame1-1"]'))
    _checkSettings.fully(true)
    await eyes.check(undefined, _checkSettings)
    }
    await eyes.close(true)
  })
})